version: "3.9"
services:
    alligator_bot:
        build: .
        volumes:
            - .:/app
        restart: always
        logging:
            driver: json-file
            options:
                max-size: 10m
                max-file: "5"
